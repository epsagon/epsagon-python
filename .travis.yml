language: python
python:
  - "2.7"
  - "3.6"
install:
  - pip install -r requirements.txt
before_script:
  - pylint --rcfile=.pylint --msg-template='{path}:{line}: [{msg_id}({symbol}) {obj}] {msg}' epsagon/
script:
  - pytest
